{
  "name": "fireplace-repairs",
  "version": "1.0.0",
  "description": "Fireplace repairs and cleaning services website",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html",
    "start": "live-server --port=3000 --open=/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}

